# Server port (default: 3100)
PORT=3100

# GCP credentials for server-side use (optional — clients can also pass their own)
# GOOGLE_APPLICATION_CREDENTIALS=/path/to/service-account.json
# GCP_PROJECT_ID=your-project-id
